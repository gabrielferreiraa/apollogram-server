type Post {
  id: ID!
  title: String!
  content: String!
  user: User!
}

extend type Query {
  posts: [Post!]!
}

extend type Mutation {
  createPost(title: String!, content: String!): Post
  deletePost(id: ID!): Post
}
